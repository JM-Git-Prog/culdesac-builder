builder: four genuinely different candidates with computed labels (d28), the choice appears on the lot - place_lot and /api/candidates answering with lots.next (d25), feat_paint so a colour on a named part is painted not refused (d26, the red door), the Phase 1 plat - STREET_LOTS L1-L16, the T junction and cross street, coming-soon signboards, MAX_HOUSES 20, the lots manifest block, per-house seeds so a new order stops re-rolling every existing house, plus POST /api/rebuild and a 20 minute UPBGE watchdog
